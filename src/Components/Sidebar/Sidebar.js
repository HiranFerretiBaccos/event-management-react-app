import styled from 'styled-components'

const Sidebar = styled.aside`
    grid-area: Sidebar;
    width: 180px;
    height: 100vh;
    background-image: linear-gradient(to bottom, rgb(63 60 72), rgb(20 10 40));
    box-shadow: 6px 12px 4px #777;
    list-style: none;
`;

export default Sidebar;