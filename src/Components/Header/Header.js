import styled from 'styled-components'

const Header = styled.header`
    grid-area: Header;
    width: 100%;
    height: 100px;
    background-image: linear-gradient(to right, rgb(66 63 76), rgb(20 10 40));
    box-shadow: 0px 6px 4px #777;
`;

export default Header;